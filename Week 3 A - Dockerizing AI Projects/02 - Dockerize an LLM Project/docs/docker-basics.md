# Docker basics

An image is a saved package containing the application and its dependencies.

A container is a running copy of an image. If source code changes, rebuild the image before starting a new container.
