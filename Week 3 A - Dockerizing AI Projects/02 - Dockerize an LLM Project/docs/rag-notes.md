# RAG notes

RAG means Retrieval-Augmented Generation. The system retrieves relevant document chunks and adds them to the model prompt.

This helps the model answer from your data instead of relying only on its training knowledge.
